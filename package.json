{
  "name": "remote-desk-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal signaling + TCP relay server for Remote Desk MVP",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
